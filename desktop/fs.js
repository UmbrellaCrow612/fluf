/*
 * Contains all code related to file or folder methods su8ch as those using fs
 */

/**
 * Registers all fs related listeners
 * @param {import("electron").IpcMain} ipcMain
 */
const registerFsListener = (ipcMain) => {};

module.exports = {
  registerFsListener,
};
