# CLI

Used to control the application via cli using a socket server where you can type cmds to do stuff with the UI


# Build

```bash
npx tsc
```

# Running 

- Build it 
- Run the `node ./dist/index.js`


# Building 

```bash
npm run build
```

- Builds the output dist
- Copies over definition files to desktop as it needs them
