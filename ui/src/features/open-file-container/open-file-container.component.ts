import { Component } from '@angular/core';

@Component({
  selector: 'app-open-file-container',
  imports: [],
  templateUrl: './open-file-container.component.html',
  styleUrl: './open-file-container.component.css'
})
export class OpenFileContainerComponent {
  /**
   * Reacts to file explorer item clicks and pushes and items file read into the container tabs by default renders and dosent show any file items 
   * and shows default home view but when there at least oen file clicked it adds it to stack
   */
}
