import { Injectable } from '@angular/core';

/**
 * Base service that holds data for a session for file x 
 * 
 * SHOULD NOT use any other service as it's a base service
 */
@Injectable({
  providedIn: 'root',
})
export class FileXInMemeoryContextService {
  
}
